import styled from 'styled-components';

const Section = styled.section`
  padding: 4rem 2rem;
  background: #f8f5f0;
`;

const Testimonials = () => (
  <Section>
    <h3>고객의 소리</h3>
    <p>고객님들의 생생한 후기</p>
  </Section>
);

export default Testimonials;
